# 更新日志

所有对外可见的变更记录于此。格式遵循 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.1.0/)，
版本号遵循[语义化版本](https://semver.org/lang/zh-CN/)。

## [Unreleased]

### Fixed
- backup: 备份导出/导入补上 users.email——此前一次「导出→导入」循环会静默清空全部用户邮箱，致找回密码与邮箱绑定失效；旧格式备份仍可导入（邮箱按空处理）；备份格式版本升至 2（v1.0.0 老代码无法导入新格式备份，属已知限制）
- smtp: 连接后不再向服务器发送空行——该空行在 Postfix 等严格服务器上触发 500 并污染后续 EHLO，使验证码/找回密码/测试邮件全部不可用；e2e 新增 mock SMTP 真实会话用例
- mail: 发送验证码增加每 IP 每小时 10 封的跨邮箱上限（发送失败同样计数），收敛免登录端点对管理员 SMTP 的中继滥用面
- db: users.email 增加部分唯一索引（空邮箱不参与），并发注册同邮箱由应用层竞态改为数据库约束兜底，冲突提示区分「邮箱已被使用」；若存量库 users.email 已存在重复非空值，升级时唯一索引创建会失败并报错（增量迁移「只增不改」的响亮暴露），需先清理重复邮箱再升级
- i18n: zh-CN 字典重复键清理；en 字典补 mail_subject_reset / err_mail_limit 缺失键（此前英文界面相关文案显示为键名）
- admin: 后台 `?tab=` 参数白名单化，非法值不再中断页面脚本初始化

## [v1.0.0] - 2026-09-18

首个公开预览版本。单容器部署（PHP 8.3 + SQLite），零第三方 PHP 依赖。

### 新增
- **分组与卡片**：无限分组、备注、拖拽排序（最近中心点预判）/ 跨组移动 / 键盘 ↑↓ 微调；移动端长按编辑
- **内外网切换**：每卡片可配外网 + 内网双地址，🌐 按钮「自动（CIDR 判定）/ 内网 / 外网」三模式即时切换（免刷新），自动模式读取 X-Forwarded-For
- **多用户**：首次访问自动创建内置管理员（admin / itswe）；开放注册可开关，注册用户均为普通用户（管理员被删光时下一个注册者自动接管）；独立注册页（可选邮箱）、用户协议、邮箱验证码找回密码；角色分级、建号、重置密码、禁用、删除
- **游客首页**：未登录可浏览推荐站点 + 搜索（中英双语两套推荐面板），可注册引导
- **服务器监控**：CPU、内存、磁盘、上下行网速、负载、运行时长，面板状态条 30s 轮询
- **Docker 管理**：容器列表、状态、端口映射、启动/停止/重启（Engine API 零依赖客户端，自动适配守护进程版本）
- **图标图库**：Dashboard Icons（4300+ webp）+ Simple Icons（3400+ svg）本地化，「从图库选择」支持搜索/集合筛选/分片加载
- **图标体系**：自动抓取 favicon（本地代理缓存）→ 本地上传 → 图库 → iconify 前缀 → 首字母，多层兜底
- **界面语言**：中文 / English 双语，默认跟随浏览器，支持 `?lang=en|zh` 无状态覆盖
- **自定义外观**：6 款预设渐变壁纸、URL 壁纸、上传壁纸；暗色模式跟随系统
- **数据**：备份导出/导入（JSON 整库）、卡片导出 CSV / Netscape HTML、iframe 内嵌预览
- **安全**：CSRF 全覆盖、bcrypt、登录限速、上传 MIME 实测 + 随机文件名、安全响应头、增量自动迁移（`PRAGMA user_version`，升级不炸库、回滚兼容）

### 测试
- 覆盖测试 55 项 + e2e 148 项（含找回密码全流程、数据导出与跨组移动回归）+ 老库迁移 8 项（`run-tests-ci.sh` 一键运行）

[v1.0.0]: https://github.com/showhoo/itswe-nav/releases/tag/v1.0.0